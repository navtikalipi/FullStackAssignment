spring:
  profiles:
    active: dev
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/portfolio_manager_dev
    username: root
    password: password

server:
  port: 8080

logging:
  level:
    root: DEBUG
    com.tnc: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
